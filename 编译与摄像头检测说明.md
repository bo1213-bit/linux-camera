# 编译与摄像头检测说明

本项目是 V4L2 相机采集 + UDP 分片发送程序：一块板子采集 MJPEG 视频帧，拆成 1400 字节的 UDP 分片发往接收端，接收端收齐重组后存成 jpg。
程序入口 `src/main.cpp`，核心模块 `v4l2app`（采集）、`udpapp`（分片收发）、`frame`（线程安全帧队列）。

---

## 一、查看摄像头有没有连接上

板子上电、摄像头接好后，按顺序用下面这几条指令排查。**关键：每条都要看输出，确认摄像头被系统识别且 `/dev/video*` 生成了。**

### 1. 看设备节点有没有生成

```bash
ls -l /dev/video*
```

- 正常应看到类似 `/dev/video0`（可能还有 video1 等）。
- 如果**没有任何 `/dev/video*`**：说明驱动没加载或硬件没被识别，继续看下面 dmesg。

### 2. 看内核有没有枚举到摄像头（USB / MIPI 都适用）

```bash
dmesg | grep -iE "video|camera|uvc|csi|mipi"
```

- USB 摄像头会看到 `uvcvideo` 驱动注册，类似：
  ```
  uvcvideo: Found UVC 1.00 device <摄像头名> ...
  input: UVC Camera ... as /devices/.../input/inputN
  ```
- MIPI 摄像头（LicheeRV Nano 的 CSI 接口）会看到 `cvitek` / `mipi_csi` / sensor 相关日志。
- 如果**这里也没任何摄像头相关输出**：检查物理连接（排线方向、是否插紧）、供电，确认设备树是否使能了 CSI。

### 3. 用 v4l2-ctl 确认设备能力（最权威）

先装工具（板子一般预装，没有的话）：

```bash
# Debian/Buildroot 环境
apt-get install v4l-utils        # 或 opkg install v4l-utils
```

列出所有视频设备：

```bash
v4l2-ctl --list-devices
```

输出类似：

```
USB Camera (usb-...):
    /dev/video0
```

查看该设备支持的格式与分辨率（**重点**，因为 `main.cpp` 里写死了 640×480 MJPEG，必须确认你的摄像头支持）：

```bash
v4l2-ctl -d /dev/video0 --list-formats-ext
```

输出会列出支持的像素格式（如 `[0]: 'MJPG'`、`[1]: 'YUYV'`）及对应分辨率。**如果摄像头只支持 YUYV 不支持 MJPG，需要改 `src/main.cpp` 第 28 行的 `V4L2_PIX_FMT_MJPEG`。**

### 4. 快速抓一帧验证摄像头真能出图（不跑程序也能测）

```bash
v4l2-ctl -d /dev/video0 --set-fmt-video=width=640,height=480,pixelformat=MJPG \
    --stream-mmap=4 --stream-count=1 --stream-to=test.jpg
```

成功后当前目录会有 `test.jpg`，拷出来用图片查看器打开能看到画面，说明摄像头硬件 + 驱动完全正常。

> 排查小结：`ls /dev/video*` → `dmesg` → `v4l2-ctl --list-devices` → `--list-formats-ext`，任一步断了就定位到那一步修。

---

## 二、编译

### 1. 依赖

- C++ 编译器 `g++`（支持 C++11）
- V4L2 头文件（`linux/videodev2.h`），随内核头自带
- pthread（Makefile 已 `-lpthread`）

Debian/Buildroot 环境没装 g++ 的话：

```bash
apt-get install g++
```

### 2. 本机直接编译（板子上或交叉机用同一套工具链时）

在项目根目录（`linux-camera/`，和 `Makefile` 同级）执行：

```bash
make          # 编译，生成 ./camera
make clean    # 清除 build/ 和 camera
```

编译产物：可执行文件 `camera`（根目录下）。Makefile 会自动建 `build/` 放中间 `.o`，头文件改动会自动触发重编（`-MMD` 生成的依赖已 include）。

### 3. 运行

```bash
make run
# 或直接
./camera
```

### 4. 交叉编译到板端（在 PC 上编译给 LicheeRV Nano 跑）

Makefile 全用 `?=` 赋值，直接在命令行覆盖编译器即可，无需改文件：

```bash
# 指向你的交叉工具链（LicheeRV Nano SDK 自带 host-tools 里的 riscv64-unknown-linux-gnu-g++）
make CXX=/path/to/riscv64-unknown-linux-gnu-g++
```

产物 `camera` 用 `scp` 或 NFS 拷到板子上运行。

### 5. 改发送目标地址

接收端 IP 在 **`src/udpapp.cpp` 第 54 行** 硬编码为 `192.168.1.100`，端口 `5004`（第 53 行）。接收端同样在 `udpapp.cpp` 的 `udpdata_rece()` 里 `bind(5004)`。两端 IP/端口要对应。

改完重新 `make` 即可。

---

## 三、常见问题速查

| 现象 | 原因 / 处理 |
|---|---|
| `open device failed` | `/dev/video0` 不存在，回第一节排查摄像头连接 |
| `capture init failed` | `QUERYCAP/S_FMT/REQBUFS/mmap/STREAMON` 某步失败；多半是分辨率/格式不支持，用 `v4l2-ctl --list-formats-ext` 核对后改 `main.cpp` 里的 width/height/pixelformat |
| 编译报 `v4l2_capability` 未定义 | 缺内核头，装 `linux-libc-dev` 或对应板子的内核头 |
| 编译报 C++11 相关 | `g++` 版本太老，升到 4.8.1 以上 |
| 接收端不出图 | 检查两端 IP/端口（`udpapp.cpp`）、网络连通性、接收端是否在跑 `udpdata_rece` |
