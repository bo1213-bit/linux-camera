# 知识盲区清单

> 用途：记录在这个「机器人远程遥控与遥测系统」项目里暴露出来、目前还不会的点。
> 后面照着这个清单逐个补。每发现一个新的盲区就追加到「待学习」里。

## 待学习

1. [ ] **UDP 视频分片与重组（总纲）** —— 一帧 MJPEG（15~40KB）远大于一个 UDP 包（约 1.4KB），必须拆成多包发送、接收端按序重组、并检测丢包。
2. [ ] **分片包头部要带哪几样信息** —— 至少需要「帧号(属于哪一帧) + 分片序号(帧内第几片) + 总片数(何时收齐)」；Q5 里你只想到"序列"，漏了"区分帧"和"收齐判定"。
3. [ ] **序号只能"检测/纠正"、不能"防止"意外** —— 序号能检测丢包、重排乱序、丢弃重复；但"防止丢包"要靠重传或 FEC（前向纠错）。Q6 里你把"检测"说成了"防止"。
4. [ ] **实时视频丢包选"丢弃"而非"重传"** —— 重传要等一个往返，晚到的帧失去实时意义；视频丢一帧只是花 1/20 秒，可容忍。这正是"视频走 UDP 不重传、控制走 TCP 要重传"的原因。Q7 你选了重传。
5. [ ] **TCP 粘包/拆包 + 帧格式要"长度字段"** —— TCP 是字节流、无消息边界，接收端必须靠帧头里的"长度字段"才能切出每条完整消息。Q8 你的帧格式漏了长度字段。
6. [ ] **TCP 之上为什么还要应用层 ACK + 超时重传** —— TCP 只保证"字节送到"，不保证"应用执行了指令"；ACK 确认的是"指令被执行"，超时重传应对"应用卡死/崩溃"。这是"传输层可靠"和"应用层确认"两层的区别。Q9 不了解。
7. [ ] **为什么要多线程（两个核心机制）** —— ① 阻塞：单线程里一个阻塞调用(等帧/等数据)会让其他任务全部停摆，控制指令会被视频拖死；② 流水线并行：采集N+1帧的同时编N、发N-1，端到端延迟≈最慢一步而非三步之和。Q10 你只说对了"丢包延迟"的症状。
8. [ ] **环形缓冲区的作用** —— 不是"大量存储"；是固定大小缓冲的循环复用（避免反复 malloc/free 和内存碎片）+ 天然 FIFO 适配生产者-消费者。Q11 理解偏了。
9. [ ] **条件变量的完整作用** —— 除了"通知唤醒"，还有让消费者在队列空时休眠、避免忙等空转烧 CPU。Q11 只答了"通知"这半。
10. [ ] **互斥锁的开销 + 无锁(lock-free)原理** —— 加锁开销=原子操作+竞争时自旋/阻塞/线程切换；无锁=用 CPU 原子指令(CAS)直接操作不阻塞。**结论：本项目"无锁队列"是过度优化，应改用 mutex+条件变量。** Q12 不了解流程。
11. [ ] **交叉编译的完整概念** —— 两块板子是两种 CPU 架构（iMX6ULL=ARM Cortex-A7，SG2002=RISC-V C906 主核），需要两套不同工具链；依赖库（libjpeg/SDL2）也要为对应架构交叉编译；机器人端还得用 Sophgo 官方 buildroot SDK。Q14 没意识到。

## 已解决（课上讲明白，供回顾）

- **带宽单位换算（Byte vs bit，×8）** —— MB/s（兆字节/秒）与 Mbps（兆比特/秒）差 8 倍；带宽习惯用 bit。
