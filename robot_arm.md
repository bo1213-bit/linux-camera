# 基于 Linux 双板协同的机器人远程遥控与遥测系统

## 一、项目概述

本项目在**两块 Linux 开发板**之间构建一套低延迟、高可靠的机器人远程遥控与遥测系统。其中：

- **机器人端（从端）**：负责采集摄像头视频，通过 V4L2 框架获取原始图像，经编码后通过 **RTP over UDP** 实时传输至控制端；同时接收控制端的指令，执行相应动作（如模拟运动、参数调节等），并定时上报系统遥测数据。
- **控制端（主端）**：负责接收并解码显示机器人端回传的视频流，同时提供指令发送接口（如键盘/触摸屏/命令行），向机器人端发送控制指令，并接收遥测数据进行显示或记录。

系统重点解决**实时视频传输的低延迟与稳定性**、**可靠的控制指令通信**以及**多线程并发处理**等问题。

---

---

## 三、关键技术方案

### 1. 视频采集（V4L2 + mmap + 多缓冲）

- 使用 V4L2 API 打开 USB 摄像头设备（`/dev/video0`）。
- 查询摄像头支持的格式、分辨率与帧率，优先选择 **YUYV 或 MJPEG**。
- 使用 `mmap` 映射内核缓冲区到用户空间，实现零拷贝采集；申请 **4 个缓冲区**，通过 `VIDIOC_QBUF`/`VIDIOC_DQBUF` 循环队列管理。
- 使用 `select`/`poll` 监听设备文件描述符，实现阻塞式等待新帧，采集线程将帧数据送入线程安全队列。

### 2. 视频编码

- 若摄像头直接输出 **MJPEG**：直接封装每帧 JPEG 数据为 RTP 包发送，简化编码环节。
- 若采集原始 YUYV 数据：
  - 若开发板支持硬件编码器（如瑞芯微 MPP、树莓派 MMAL），调用相应 API 进行 **H.264 硬编码**，降低 CPU 占用。
  - 否则使用 **x264 软件编码**，设置为低延迟模式（如 `zerolatency`），输出 H.264 码流。
- 编码后的码流按 **H.264 NALU** 或 **JPEG 帧** 送入发送队列。

### 3. 网络传输（RTP over UDP）

- 将压缩视频码流打包为 RTP 包：
  - 对于 H.264：按 RFC 6184 分片打包 NALU；
  - 对于 MJPEG：每个 JPEG 帧作为一个 RTP 包（或分片）。
- 使用 **UDP socket** 发送，目标地址为控制端 IP 和端口。
- 在发送端设置 RTP 头部字段（序号、时间戳、负载类型），接收端据此进行缓存重排和丢包检测。
- 为提升抗丢包能力，可预留 **FEC（前向纠错）** 或简单重传机制接口（暂不实现可根据需要扩展）。

### 4. 控制指令通道（自定义二进制协议 + TCP）

- 设计二进制协议帧格式：









- 机器人端作为 TCP 服务器，控制端作为客户端连接。
- 支持指令示例：
- `0x01`：启动/停止视频流
- `0x02`：设置分辨率/帧率
- `0x03`：模拟运动控制（前后左右）
- `0x04`：请求遥测数据
- 使用 CRC16 校验保证数据完整性；支持超时重传和应答机制（ACK）。

### 5. 遥测数据上报

- 机器人端定时（如每 1 秒）采集系统信息：CPU 使用率、内存占用、CPU 温度、网络延迟等。
- 通过 TCP 控制通道或单独 UDP 通道上报给控制端，格式同样采用自定义二进制协议（增加遥测数据类型字段）。
- 控制端解析并显示。

### 6. 多线程与并发设计

- 机器人端线程：
- 视频采集线程、视频编码线程、RTP 发送线程、控制指令接收线程、遥测采集与发送线程。
- 线程间使用 **无锁队列（环形缓冲区）** 传递帧数据，避免频繁加锁，降低延迟。
- 控制端线程：
- RTP 接收线程、视频解码显示线程、控制指令发送线程、遥测接收线程。
- 使用条件变量协调生产者-消费者。

---

## 四、预期成果与亮点

- 实现 **720p@30fps** 视频实时回传，端到端延迟 **< 200ms**（局域网环境）。
- 可靠的控制指令传输，响应时间 **< 50ms**。
- 掌握 **V4L2 底层采集、RTP 协议打包、自定义二进制协议设计、多线程并发** 等嵌入式 Linux 核心技能。
- 可扩展至真实机器人平台，只需替换指令执行模块即可。

---

## 五、开发工具与环境

- **开发板**：两块 Linux 开发板（如树莓派、香橙派、瑞芯微板卡），内核 4.x/5.x。
- **摄像头**：UVC 免驱 USB 摄像头（支持 MJPEG 或 YUYV）。
- **交叉编译工具链**（如 arm-linux-gnueabihf-gcc）或板载编译。
- **依赖库**：`libv4l2`、`libx264`（可选）、`libjpeg`（可选）、`SDL2`/`OpenCV`（用于显示，控制端）。
- **调试工具**：Wireshark、v4l2-ctl、perf。